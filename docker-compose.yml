version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:15-alpine
    container_name: code-reviewer-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: code_reviewer
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - code-reviewer-network

  # Backend API (uncomment when ready to use Docker for development)
  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   container_name: code-reviewer-backend
  #   environment:
  #     DATABASE_URL: postgresql://postgres:postgres@postgres:5432/code_reviewer
  #     ENVIRONMENT: development
  #   ports:
  #     - "8000:8000"
  #   volumes:
  #     - ./backend:/app
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
  #   networks:
  #     - code-reviewer-network

  # Frontend (uncomment when ready to use Docker for development)
  # frontend:
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   container_name: code-reviewer-frontend
  #   environment:
  #     VITE_API_BASE_URL: http://localhost:8000
  #   ports:
  #     - "5173:5173"
  #   volumes:
  #     - ./frontend:/app
  #     - /app/node_modules
  #   command: npm run dev -- --host
  #   networks:
  #     - code-reviewer-network

volumes:
  postgres_data:
    driver: local

networks:
  code-reviewer-network:
    driver: bridge
